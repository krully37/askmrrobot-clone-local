# Create a Blizzard developer application, then set these as user environment variables
# or copy this file to .env for use by external tooling. The dashboard reads process env only.
BLIZZARD_CLIENT_ID=
BLIZZARD_CLIENT_SECRET=
BLIZZARD_REGION=us
BLIZZARD_LOCALE=en_US
